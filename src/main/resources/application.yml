spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://tupldatabase.cpqi94mihqjg.ap-northeast-2.rds.amazonaws.com:3306
    username: tupl_admin
    password: likelion

  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true

  output:
    ansi:
      enabled: always

logging:
  level:
    org.hibernate.SQL: debug

jwt:
  header: Authorization
  token-validity-in-seconds: 86400 # 24시간